const Header = () => {
  const template = `
        <header>
            <h1>My Parcel app</h1>
            <p> This is a boilerplate for a simple JS workflow using parcel </p>
        </header>
    `;

  return template;
};

export default Header;
